{
  "dependencies": {
    "espn-fantasy-football-api": "^0.13.0",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node ./src/server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  }
}
